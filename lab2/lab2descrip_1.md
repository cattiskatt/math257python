## CHW2.1. Vector Operations: Find missing vector

Recall that a *linear combination* of three vectors **u, v, w** is any other vector **z** defined as a sum of scalar multiples **z** = α**u** + β**v** + γ**w**. If the vectors **u, v, w** are linearly independent, then any such linear combination will result in a different sum if at least one of the scalar coefficients α,β,γ is different.

In this case, three vectors **u, v, w** were linearly combined as above. The vectors **u** and **v**, the sum **z**, and the coefficients α,β,γ have been given to you, but the value of the vecotr **w** has been lost. Write code to calculate the missing vector **w**.

The setup code gives the following variables:

## CHW2.1. Vector Operations: Find missing vector

Recall that a *linear combination* of three vectors **u, v, w** is any other vector **z** defined as a sum of scalar multiples **z** = α**u** + β**v** + γ**w**. If the vectors **u, v, w** are linearly independent, then any such linear combination will result in a different sum if at least one of the scalar coefficients α,β,γ is different.

In this case, three vectors **u, v, w** were linearly combined as above. The vectors **u** and **v**, the sum **z**, and the coefficients α,β,γ have been given to you, but the value of the vecotr **w** has been lost. Write code to calculate the missing vector **w**.

The setup code gives the following variables:

| Name | Type | Description |
|------|------|-------------|
| u | 1d numpy array | vector u |
| v | 1d numpy array | vector v |
| z | 1d numpy array | vector z |
| alpha | float | coefficient alpha |
| beta | float | coefficient beta |
| gamma | float | coefficient gamma |

Your code snippet should define the following variables:

| Name | Type | Description |
|------|------|-------------|
| w | 1d numpy array | vector w |